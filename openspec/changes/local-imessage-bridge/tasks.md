# 任务：local-imessage-bridge

## 阶段一：项目骨架与配置模型

- [x] 初始化项目目录结构与运行入口
- [x] 定义配置文件格式，覆盖白名单联系人、默认 workspace、拒绝回复文案、消息合并窗口
- [x] 定义本地状态存储格式，覆盖联系人线程映射、消息去重记录、附件暂存元数据
- [x] 增加配置加载与启动校验，确保缺失字段和非法路径能被明确报错

## 阶段二：iMessage 传输层接入

- [x] 封装 `imsg` 适配层，支持接收入站消息事件
- [x] 封装 `imsg` 出站发送能力，支持向指定会话回复文本
- [x] 标准化 iMessage 消息对象，统一联系人标识、消息 ID、时间戳、文本和附件元数据
- [x] 增加 `imsg` 可用性检测与启动失败处理

## 阶段三：白名单与回环保护

- [x] 实现白名单联系人匹配逻辑
- [x] 实现非白名单固定回复逻辑
- [x] 实现本机自发消息过滤，避免 bridge 处理自己发出的回复
- [x] 实现入站消息去重，避免同一消息被重复提交

## 阶段四：连续消息合并

- [x] 实现按联系人分组的短窗口消息缓冲区
- [x] 实现窗口内多条文本消息按时间顺序合并
- [x] 实现窗口内多条图片附件与文本共同合并
- [x] 增加窗口超时触发提交与缓冲区清理逻辑

## 阶段五：Codex 集成

- [x] 基于已导出的 `app-server` 协议实现本地客户端封装
- [x] 实现 `app-server` 客户端层，支持创建线程、向线程追加消息、等待响应
- [x] 实现联系人到独立 Codex 线程的持久化映射
- [x] 实现联系人默认 workspace 绑定，并确保提交时在正确目录下执行
- [x] 设计并实现 `app-server` 不可用时的降级与恢复策略

## 阶段六：图片输入链路

- [x] 实现图片附件筛选与本地暂存
- [x] 实现图片暂存文件与联系人、消息、线程的关联记录
- [x] 实现向 Codex 提交图片输入的桥接逻辑
- [x] 实现图片暂存失败时自动降级为纯文本处理

## 阶段七：主循环与错误恢复

- [x] 串联完整主循环：入站消息、策略判断、缓冲、Codex 提交、出站回复
- [x] 实现 Codex 调用失败时的用户可读错误回复
- [x] 实现发送失败、状态损坏、临时文件异常的日志与恢复策略
- [x] 增加进程重启后的状态恢复能力

## 阶段八：验证与运维

- [x] 为配置解析、白名单匹配、消息合并、状态存储编写自动化测试
- [x] 为 `imsg` 适配层和 Codex 集成层增加最小可运行的集成验证
- [x] 编写本机部署与权限配置说明
- [x] 编写第一版使用说明和已知限制

## 阶段九：第二版管理员命令

- [x] 增加管理员联系人配置，并兼容旧配置
- [x] 实现管理员 iMessage 命令解析与帮助提示
- [x] 实现通过 iMessage 动态增删白名单联系人
- [x] 实现通过 iMessage 动态修改联系人 workspace 映射
- [x] 实现动态联系人变更的本地状态持久化与重启恢复
- [x] 调整 watch 策略以支持动态联系人在运行时生效
- [x] 为管理员命令、动态状态变更和运行时回路补充自动化测试

## 当前未决事项

- [x] 确定 `app-server client` 的具体实现方式：直接 WebSocket/stdio JSON-RPC 封装，还是优先复用生成的 TypeScript 类型与协议层
